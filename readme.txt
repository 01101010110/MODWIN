Run build.bat in MODWIN to build the missing ISO, MOD, PATH, and WIM folders. 
You are now able to paste your window iso's contents into the the ISO folder of MODWIN.
Run the script representing the version of windows you are using.
Else, use the batches in the TOOLS folder to manually mount and modify your image
